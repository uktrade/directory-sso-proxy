{
  "host_env_var_prefix": "SSO_PROXY",
  "file_path": "./docker/.env.test",
  "env_vars": {
    "required": [
      "PORT",
      "SECRET_KEY",
      "SIGNATURE_SECRET",
      "SSO_SIGNATURE_SECRET",
      "SSO_UPSTREAM"
    ],
    "optional": [
      "DEBUG",
      "CODECOV_REPO_TOKEN"
    ]
  }
}
